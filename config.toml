# The URL the site will be built for
base_url = "https://maxild.github.io"

title = "M's Scratch Buffers (WIP)"

# Whether to automatically compile all Sass files in the sass directory
compile_sass = true

# Whether to do syntax highlighting
# Theme can be customised by setting the `highlight_theme` variable to a theme supported by Zola
highlight_code = true
highlight_theme = "inspired-github"

# Whether to build a search index to be used later on by a JavaScript library
build_search_index = false

theme = "brutal"

taxonomies = [
    {name = "tags"},
]
#taxonomies = [
#    {name = "categories", rss = true, paginate_by=5},
#    {name = "tags", rss = true, paginate_by=5},
#]

[extra]
even_menu = [
    {url = "$BASE_URL", name = "Home"},
    {url = "$BASE_URL/tags", name = "Tags"},
    # TODO: Create about page
    #{url = "$BASE_URL/about", name = "About"},
]
#TODO: $BASE_URL is replaced by congig.base_url in the Even themes template, and
# because out base_url ends in / we we
#even_menu = [
#    {url = "$BASE_URL", name = "Home"},
#    {url = "$BASE_URL/tags", name = "Tags"},
#    {url = "$BASE_URL/about", name = "About"},
#]
even_title = "M's Scratch Buffers (WIP)"
